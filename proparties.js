const bottle = {
    color: 'White',
    hold: 'Water',
    price: 100,
    isCleaned: true
};

const keys = Object.keys(bottle);
const values = Object.values(bottle);
console.log(keys);
console.log(values);